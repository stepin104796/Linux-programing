#ifndef __MYUTILS_H
#define __MYUTILS_H

#include<stdio.h>
#include<string.h>
#include <stdarg.h>

int multiplyNumbers(int n);
int prime(int n);
int sum(int N, ...);
int checkpalindrome(char *s);
#endif
